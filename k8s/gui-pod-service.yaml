apiVersion: v1
kind: Service
metadata:
  name: gui-pod-service
  namespace: default
spec:
  type: NodePort
  selector:
    user: user1
  ports:
    - port: 6901
      targetPort: 6901
      protocol: TCP
      nodePort: 30680
